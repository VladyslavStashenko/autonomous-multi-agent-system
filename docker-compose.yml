services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - ./.agent_memory:/app/.agent_memory
      - ./.agent_state:/app/.agent_state
